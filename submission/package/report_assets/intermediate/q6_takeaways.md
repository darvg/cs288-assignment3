# Takeaways

Hybrid retrieval improves robustness over either sparse-only or dense-only retrieval. The main future work is replacing the sample corpus with the full EECS crawl and running the full ablation matrix on the released reference corpus.
